[package]
edition = "2021"
name = "raw_index"
version = "0.0.0"

[dependencies]
arena = {path = "../arena"}
cst = {path = "../cst"}
syntax = {path = "../syntax"}
text-size = "1.1.0"

[dev-dependencies]
parser = {path = "../parser"}
test_utils = {path = "../test_utils"}
